package WalkingSimulator;

public class Walk {
    private static int walkCounter;
    public void walk(){
        walkCounter = (int) (Math.random()*100 + 1);
        for (int i = 0; i < walkCounter ; i++){
            
        }
    }
}
